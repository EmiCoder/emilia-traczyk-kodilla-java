create index TITLEON on BOOKS (TITLE);
EXPLAIN SELECT COUNT(*)
FROM BOOKS
WHERE TITLE = 'The Stranger';

create index READERON on readers(FIRSTNAME, LASTNAME);
EXPLAIN SELECT COUNT(*)
FROM READERS
WHERE FIRSTNAME = 'John'
and  LASTNAME = "Smith";
